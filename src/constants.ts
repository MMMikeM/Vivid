import colors from "tailwindcss/colors";
const orange = colors.orange[500];
const pink = colors.pink[500];
const blue = colors.blue[500];
const green = colors.green[500];
const indigo = colors.indigo[500];
const lightpurple = colors.purple[300];
const white = colors.white;
const background = colors.slate[800];

export const theme = {
  orange,
  pink,
  blue,
  green,
  indigo,
  lightpurple,
  white,
  background,
};
